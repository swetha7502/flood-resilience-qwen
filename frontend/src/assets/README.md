# Assets

Place `flood-map-bg.png` (aerial storm-scene photo) in this directory.
It is referenced by the neighborhood map component (Step 4) as:

  import floodMapBg from './assets/flood-map-bg.png';

and used as a CSS background-image.
